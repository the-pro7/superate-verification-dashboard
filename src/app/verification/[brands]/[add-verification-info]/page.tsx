import React from 'react'
import styles from "./page.module.css"

const Page = () => {
  return (
    <main className={`${styles.container}`}>
      <h2>Add Brand Verification Detail</h2>
    </main>
  )
}

export default Page